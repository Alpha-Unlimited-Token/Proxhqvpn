kind = "web"
previewPath = "/"
title = "GhostNet VPN"
version = "1.0.0"
id = "artifacts/ghost-vpn"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24043

[services.development]
run = "pnpm --filter @workspace/ghost-vpn run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/ghost-vpn", "run", "build" ]
publicDir = "artifacts/ghost-vpn/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24043"
BASE_PATH = "/"
