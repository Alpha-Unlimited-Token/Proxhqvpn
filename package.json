{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "audit:capabilities": "pnpm --filter @workspace/api-server exec esbuild ../../scripts/audit-route-capabilities.ts --bundle --platform=node --format=esm --alias:@workspace/capabilities=../../lib/capabilities/src/index.ts --outfile=/tmp/proxhq-audit-cap.mjs --log-level=silent && node /tmp/proxhq-audit-cap.mjs",
    "audit:all-capabilities": "pnpm run audit:capabilities",
    "audit:api-routes": "pnpm --filter @workspace/scripts exec tsx ./audit-api-route-inventory.ts",
    "audit:frontend-routes": "pnpm --filter @workspace/scripts exec tsx ./audit-frontend-routes.ts",
    "audit:production-env": "pnpm --filter @workspace/scripts exec tsx ./audit-production-env.ts",
    "audit:release": "pnpm run audit:all-capabilities && pnpm run audit:api-routes && pnpm run audit:frontend-routes && pnpm run audit:production-env",
    "ci": "pnpm run audit:all-capabilities && pnpm run audit:api-routes && pnpm run audit:frontend-routes && pnpm run typecheck && pnpm -r --if-present run build",
    "dev:security-console": "pnpm --filter @workspace/security-console dev",
    "build:security-console": "pnpm --filter @workspace/security-console build",
    "dev:security-api": "pnpm --filter @workspace/api-server dev:security-api",
    "build:security-api": "pnpm --filter @workspace/api-server build:security-api"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "stripe": "20.0.0",
    "stripe-replit-sync": "1.0.0"
  }
}
